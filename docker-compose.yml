services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: estatemint-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_HOST: postgres
      DATABASE_NAME: estatemint
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/estatemint?schema=public
      REDIS_HOST: redis
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - estatemint-network

  postgres:
    image: postgres:17-alpine
    container_name: estatemint-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: estatemint
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d estatemint"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - estatemint-network

  redis:
    image: redis:8-alpine
    container_name: estatemint-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - estatemint-network

volumes:
  postgres_data:
  redis_data:

networks:
  estatemint-network:
    driver: bridge
